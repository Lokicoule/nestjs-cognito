---
"@nestjs-cognito/testing": patch
---

Export `CognitoTestingService`, `CognitoMockService` and the `MockConfig`, `MockUserConfig` and `TokenPayload` types used in the README, and fix the README examples that did not match the API.
